"""Service layer: use cases orchestrating domain logic and infrastructure.

The UI talks to services; services coordinate domain rules with
infrastructure (memory providers, database, filesystem).
"""
